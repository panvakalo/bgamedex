{
  "name": "bgamedex",
  "private": true,
  "scripts": {
    "dev": "concurrently -n backend,frontend -c blue,green \"cd backend && npm run dev\" \"cd frontend && npm run dev\"",
    "seed": "cd backend && npm run seed",
    "build": "cd frontend && npm run build",
    "test:e2e": "playwright test",
    "test": "cd frontend && npm test && cd ../backend && npm test",
    "test:coverage": "cd frontend && npm run test:coverage && cd ../backend && npm run test:coverage"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "@types/better-sqlite3": "^7.6.13",
    "better-sqlite3": "^12.6.2",
    "concurrently": "^9.1.2"
  }
}
